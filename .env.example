# 이 파일을 .env로 복사한 뒤 값을 채우세요. .env는 절대 커밋하지 마세요.

# --- 관리자 계정 (필수) ---
# 최초 실행 시 12자 이상이어야 합니다.
ADMIN_PASSWORD=
ADMIN_USERNAME=admin

# HTTPS 리버스 프록시 뒤에서 운영할 때 true
COOKIE_SECURE=false

# --- Google 로그인 (선택) ---
# Google Cloud Console > API 및 서비스 > 사용자 인증 정보에서
# "OAuth 2.0 클라이언트 ID"(웹 애플리케이션)를 만들고,
# 승인된 리디렉션 URI에 아래 GOOGLE_REDIRECT_URI와 같은 값을 등록하세요.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 비워두면 요청 호스트에서 유추합니다. 리버스 프록시 뒤에서는 명시하는 편이 안전합니다.
# 예: https://ssh.example.com/api/auth/google/callback
GOOGLE_REDIRECT_URI=

# 로그인을 허용할 계정. 둘 중 하나 이상을 반드시 지정해야
# Google 로그인이 켜집니다 (없으면 서버가 자동으로 비활성화).
# 쉼표로 여러 개를 나열할 수 있습니다.
GOOGLE_ALLOWED_EMAILS=
GOOGLE_ALLOWED_DOMAINS=
